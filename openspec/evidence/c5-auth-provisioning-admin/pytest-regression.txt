# C5 全量非 PG 回归（任务 5.4 / change task 5.4）——2026-09-14，工作树为 C5 特性分支

命令：`python -m pytest -q -W error tests/`
结果：`1181 passed, 192 skipped in 374.57s (0:06:14)`，退出码 0

- `-W error` 开启，无 warning 提升为 error 导致的失败。
- 本次重跑于新增 `test_session_timeout.py` **之后**的当前树，取代早前
  1174 passed / 186 skipped 的记录（该记录取自基线 merge 之前的工作树）。
  两处计数变化均已定位，均非回归：
  - **+7 passed**：基线 merge（`35fea3d3`）带入 `origin/main` 的新测试
    —— `tests/test_provider_codex_protocol.py` +6、`tests/test_runtime_smoke.py` +1
    （`git diff f99e2a92 HEAD -- tests/` 新增测试函数恰为 7 个）。
  - **+6 skipped**：本次新增的 `tests/auth_provisioning/test_session_timeout.py`
    6 项带 `pytest.mark.postgres`，在非 PG 运行下按预期跳过；其真实执行见
    `pytest-pg-integration.txt`（53 passed）。
- C5 的 26 项 PG 集成在非 PG 运行下整体跳过，真实执行见 `pytest-pg-integration.txt`。
- 计数含 C5 非 PG 单元 27 项（crypto/ws_guard/cli_layer/auth_config，
  见 `pytest-auth-unit.txt`）。
